2) 관리자 로그인 모달 -> 로그인 완료 시 모달 삭제






+>페이지네이션 길이 조절



=====완=====
>로그인 페이지에서 로그인 완료 후, 뒤로가기가 안 됩니다.
```
import qs from 'query-string';

//NOTE: query를 객체로 변환하는 코드
const query = qs.parse(locations.search);

//NOTE: location의 state를 이용해서 분기처리
if (query.prev) {
    query.prev.includes('register')
    ? navigate('/')
    : navigate(query.prev);
}
```

>일괄 삭제 시, alert 출력 되게 만들기
    1. 삭제 버튼(일괄 삭제) : alert출력 x. 리랜더링은 되는 상황
    >>await 추가로 출력 및 리랜더링 됨
    2. 삭제 시, 헤더의 수 변경
    >>recoil로 backOfficeTotalCount를 따로 만들어서 해결
        -해결방법
        1) 부모 컴포넌트에서 state를 body와 header에 공유 or 부모 컴포넌트에서 
           totalCount를 불러오는 함수를 선언
        2) totalCount 자체를 recoil로 변환
        3) react-query 데이터 패칭 라이브러리 (SWR)

수정 기능 구현 -완
    1. 체크박스 및 버튼을 눌렀을 때, 체크박스가 포함된 줄의 index 정보를 받아야함
    HOW? index만 받으면 data처리 가능, index를 어떻게 받아야?
    ```
    const [SelectedIDs, setSelectedIDs] = useState([]);
    const [SelectIndex, setSelectIndex] = useState();
    const showModal = (item, index) => {
        return()=>{
            setSelectedIDs([item.id]);
            setSelectIndex(index);
            setModalOpen(true);
        }
    };
    onClick={showModal(item, index)} 
    ```

페이지네이션 완
    현재 페이지 표시 완
    검색시 전체 검색 수 많큼만 page로 완
    컴포넌트로 빼면 좋음-완
    페이지네이션 max 못 넘어가게 완