import React from "react";
import styles from "./home.module.scss";

const Home = () => {
  return (
    <section className={styles.wrapper}>
      <div>순위</div>
      <div>카테고리</div>
    </section>
  );
};

export default Home;
