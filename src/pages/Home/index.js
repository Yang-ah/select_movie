import React, { useState } from "react";

import styles from "./home.module.scss";

const Home = () => {
  return (
    <section className={styles.wrapper}>
      메인페이지
    </section>
  );
};

export default Home;