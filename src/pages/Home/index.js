import React, { useState } from "react";

import styles from "./home.module.scss";

const Home = () => {
  return (
    <section className={styles.wrapper}>
      <div className={styles.sectionBody}>순위</div>
    </section>
  );
};

export default Home;